一个页上有多个栈帧，每个栈帧下保存了当前调用的信息
fp - 8 位置固定存储了上一个栈帧的位置
fp - 16 位置固定存储了当前调用的返回值的位置

user trap 调用时，直接把当前 PC 修改到 handler 的位置
为了从 handler 中回到正常运行，需要保存调用 handler 时的栈帧
sigreturn 函数由于是 syscall，返回值会覆盖当前的 rax 寄存器，直接把这个返回值设成 rax 的值即可